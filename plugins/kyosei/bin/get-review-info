#!/usr/bin/env bash
set -euo pipefail
script_dir="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
target="${script_dir}/../dist/bin/get-review-info.js"
[ -f "${target}" ] || "${script_dir}/build-kyosei"
exec node "${target}" "$@"
