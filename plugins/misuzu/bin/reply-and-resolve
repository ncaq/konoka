#!/usr/bin/env bash
set -euo pipefail
script_dir="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
target="${script_dir}/../dist/bin/reply-and-resolve.js"
[ -f "${target}" ] || "${script_dir}/build-misuzu" "${target}"
exec node "${target}" "$@"
