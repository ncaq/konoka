#!/usr/bin/env bash
set -euo pipefail
script_dir="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
target="${script_dir}/../dist/bin/read-pull-request-template.js"
[ -f "${target}" ] || "${script_dir}/build-pr"
exec node "${target}" "$@"
